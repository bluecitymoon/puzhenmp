<view class="container">
  <view class="page-body">
    <view wx:for="{{ads}}"  wx:key="id">
          <image style='width:{{item.rpxWidth}}rpx; height:{{item.rpxHeight}}rpx'  src='{{item.content}}'></image>
    </view>
  </view>
</view>